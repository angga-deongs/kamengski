//-------------------------------------------------------------------------------
  @name: Product
  -------------------------------------------------------------------------------//

//product
.product#product
  .container
    .product__filter
      ='\n'
      button.product__filter__btn.btn.btn--primary.js-product-filter(type='button') filter product: SHORT SLEVEE TEES (200)
    .product__list.js-product-list
      each val in dataProduct.list
        -
          data = val
        include ../components/product.pug
    include ../components/pagination.pug
